# 🎨 MarioTrickster 工业化美术管线操作菜单 (V4.2 效率优化版)

> **💡 核心护城河：彻底消灭"本地调参内耗"**
> 实体管 What，抽屉管 How。Manus 是大脑（啃书定规则、记设定、算节点参数）；你只是无情的渲染终端（复制、填入 ComfyUI、跑图验收）。图坏了绝不自己拉滑块，直接丢给 Manus 重新发牌！

---

## 🛠️ 步骤 0：本地渲染终端准备清单 (仅需做一次)

在给 Manus 发指令前，请确保你的本地厨房已经配齐了以下工具。如果已经搞定，请直接跳到【每日开工启动】。本指南基于 2026 年最新稳定版环境编写，针对 **RTX 4070 (12GB VRAM)** 显卡优化。

> **📂 路径约定**
> 本文档中所有路径以 `{ComfyUI根目录}` 代替你实际的安装位置。请根据你的实际情况替换，例如：
> - 如果你解压到了 `E:\ComfyUI\ComfyUI_windows_portable_nvidia\ComfyUI_windows_portable`，那么 `{ComfyUI根目录}` = 这个路径
> - 如果你解压到了 `D:\ComfyUI_windows_portable`，那么 `{ComfyUI根目录}` = `D:\ComfyUI_windows_portable`

> **💻 你的硬件配置评估**
> RTX 4070 拥有 12GB VRAM，完全满足 SDXL 模型的运行需求。实测可以在 1024×1024 分辨率下流畅跑图，同时叠加 IPAdapter + ControlNet 也不会爆显存。是运行本项目全部管线的理想配置。

### 0. 前置工具（确认已安装）

在开始安装 ComfyUI 之前，请确保以下两个基础工具已安装到你的 Windows 系统中。

| 工具 | 用途 | 下载地址 | 安装说明 | ✅ 已装？如何确认 |
|------|------|----------|----------|------------------|
| **7-Zip** | 解压 ComfyUI 的 `.7z` 压缩包 | [7-zip.org](https://www.7-zip.org/) | 下载 64-bit Windows x64 版本，双击安装，一路下一步即可 | 右键任意文件，弹出菜单中能看到 `7-Zip` 选项就说明已装，跳过 |
| **Git** | 克隆 ComfyUI Manager 和插件 | [git-scm.com](https://git-scm.com/downloads) | 下载 Windows 版，安装时全部保持默认选项即可 | 按 `Win + R` 输入 `cmd` 回车打开黑框，输入 `git --version` 回车。如果显示类似 `git version 2.xx.x` 的文字就说明已装，跳过。如果显示 `不是内部或外部命令` 则需要安装 |

### 1. 核心引擎与大模型

**1.1 安装 ComfyUI (Portable 版)**

> **✅ 已装确认**：如果你电脑里已经有 `ComfyUI_windows_portable` 文件夹，且双击 `run_nvidia_gpu.bat` 能正常打开网页，请直接跳过本节。

推荐使用官方的 Windows 一键解压版，它自带了独立的 Python 3.13 和 CUDA 13.0 环境，无需配置复杂的系统依赖。你的 RTX 4070 完美支持 CUDA 13.0。

*   **下载地址**：前往 [GitHub Releases](https://github.com/Comfy-Org/ComfyUI/releases) 或 [ComfyUI 官方下载页](https://www.comfy.org/download)，下载 **`ComfyUI_windows_portable_nvidia.7z`**（不带 cu126 后缀的默认版，约 1.77GB）。

    > ⚠️ **不要下载** `ComfyUI_windows_portable_nvidia_cu126.7z`。那是给老显卡/老驱动的降级兼容包。你的 RTX 4070 用默认版性能更好。
*   **解压与启动**：
    1.  右键点击下载的 `.7z` 文件 → 选择 `7-Zip` → `解压到“ComfyUI_windows_portable_nvidia\”`。
    2.  将解压出的文件夹移动到剩余空间大于 **50GB** 的固态硬盘（如 `E:\ComfyUI\` 下）。避免放在桌面或含中文的路径下。解压后的文件夹路径即为你的 `{ComfyUI根目录}`。
    3.  进入文件夹，双击运行 `run_nvidia_gpu.bat`。
    4.  首次启动会自动下载必要组件，可能需要 2–5 分钟。完成后控制台会显示 `To see the GUI go to: http://127.0.0.1:8188`，浏览器会自动打开。
*   **硬件要求**：

| 项目 | 最低要求 | 你的配置 (RTX 4070) | 备注 |
|------|----------|----------------------|------|
| GPU | NVIDIA 2018年+ | RTX 4070 (Ada Lovelace) | 完美支持 |
| VRAM | 8GB | 12GB | SDXL 模型舒适运行，叠加 ControlNet + IPAdapter 无压力 |
| 系统内存 | 16GB | 建议 32GB | 内存不足时模型加载会明显变慢 |
| 硬盘空间 | 50GB | 建议预留 80GB+ | ComfyUI 本体 ~2GB，模型文件会占大量空间 |
| 显卡驱动 | 最新稳定版 | 前往 [NVIDIA 驱动下载](https://www.nvidia.com/download/index.aspx) 更新 | 驱动过旧可能导致 CUDA 13.0 无法启动 |

**1.2 安装 ComfyUI Manager**

> **✅ 已装确认**：启动 ComfyUI 后，如果网页**右上角**能看到绿色的 `Manager` 按钮，请直接跳过本节。

这是 ComfyUI 的"应用商店"，用于一键安装缺失节点和更新插件。

*   **安装方法**：打开命令提示符（按 `Win + R`，输入 `cmd`，回车），依次执行以下命令：
    ```bash
    cd /d {ComfyUI根目录}\ComfyUI\custom_nodes
    git clone https://github.com/ltdrdata/ComfyUI-Manager.git
    ```
    > ℹ️ 如果提示 `git 不是内部或外部命令`，说明 Git 未安装或未加入系统 PATH。请回到步骤 0 安装 Git。
*   **验证**：关闭 ComfyUI 控制台黑框，重新双击 `run_nvidia_gpu.bat` 启动。在浏览器界面**右上角**，你应该能看到绿色的 `Manager` 按钮。点击它就能看到插件管理界面。

**1.3 下载大模型 (Checkpoint)**

> **✅ 已装确认**：检查 `ComfyUI\models\checkpoints\` 目录下是否已有 `sd_xl_base_1.0.safetensors` 或其他 SDXL 模型（文件大小通常在 6GB 以上）。

本项目基于 SDXL 架构，你需要下载基础模型或风格化微调模型。单个 Checkpoint 文件约 6–7GB。

| 模型名称 | 类型 | 文件大小 | 下载地址 | 说明 |
|----------|------|----------|----------|------|
| `sd_xl_base_1.0.safetensors` | 官方底模 | ~6.9GB | [HuggingFace 直链](https://huggingface.co/stabilityai/stable-diffusion-xl-base-1.0/resolve/main/sd_xl_base_1.0.safetensors) | 必装。所有 SDXL 工作流的基础 |
| `Animagine XL V3` | 二次元微调 | ~6.5GB | [Civitai 搜索](https://civitai.com/models/260267) | 推荐。动漫/手绘风格效果极佳 |
| `AAM XL AnimeMix` | 二次元微调 | ~6.5GB | [Civitai 搜索](https://civitai.com/) | 备选。另一种动漫风格取向 |

*   **放置路径**：将下载的 `.safetensors` 文件放入 `{ComfyUI根目录}\ComfyUI\models\checkpoints\` 目录下。
*   **下载提示**：文件较大，建议使用迅雷或 IDM 等下载工具加速。HuggingFace 直链可直接粘贴到下载工具中。
*   **放完后验证**：
    1.  如果 ComfyUI 正在运行，关闭控制台黑框，重新双击 `run_nvidia_gpu.bat` 启动（新放入的模型需要重启才能识别）。
    2.  在浏览器界面中，在画布空白处双击 → 搜索 `Load Checkpoint` → 拖出节点。
    3.  点击节点上的模型下拉菜单，应该能看到你刚放入的 `sd_xl_base_1.0.safetensors`。如果看到了，说明模型已正确识别，本步完成。
    4.  如果下拉菜单为空，请检查文件是否放对了目录（必须是 `models\checkpoints\` 而不是 `models\` 根目录）。

### 2. 必备插件与模型配置

以下插件均通过 ComfyUI Manager 一键安装。如果你第一次用 Manager 装插件，请按以下步骤操作：

> **📖 Manager 安装插件通用流程（每个插件都是这个流程）**
> 1. 点击网页**右上角**绿色的 `Manager` 按钮 → 弹出管理面板
> 2. 点击 `Custom Nodes Manager`（自定义节点管理器）
> 3. 在顶部搜索框中输入插件名称（如 `IPAdapter`）
> 4. 在搜索结果中找到目标插件，点击右侧的 `Install` 按钮
> 5. 等待安装完成（状态变为 `Installed`）
> 6. 所有插件装完后，点击面板底部的 `Restart` 按钮重启 ComfyUI（可以装完所有插件再一次性重启）

**2.1 IPAdapter Plus (前期垫图探索专用)**

> **✅ 已装确认**：
> 1. 插件：在 ComfyUI 画布空白处双击，搜索 `IPAdapter Advanced`，能搜到说明插件已装。
> 2. 模型：检查 `models\clip_vision\` 下是否有 `CLIP-ViT-H-14...`，以及 `models\ipadapter\` 下是否有 `ip-adapter-plus_sdxl...`。

用于将参考图的风格迁移到新图中。**⚠️ 警告：IPAdapter 本质是“特征借用”，具有概率性。在生成连续的【人物动画序列帧】时，会导致致命的特征漂移和帧间闪烁。因此，它仅限在「菜单2」的探索期使用，严禁用于「菜单3」的量产期。**

*   **插件安装**：按上方「通用流程」操作，搜索框输入 `IPAdapter`，找到 **ID 3** 的 `ComfyUI_IPAdapter_plus`（作者：**Matteo**，带蓝色认证标，37 nodes），点击 `Install`。注意不要装带 `fork` 或 `V2` 后缀的仿品。
*   **下载配套模型**：插件本身只是代码，还需要下载以下模型文件才能工作：

| 模型文件 | 放置目录 | 文件大小 | 下载地址 |
|----------|----------|----------|----------|
| `CLIP-ViT-H-14-laion2B-s32B-b79K.safetensors` | `ComfyUI\models\clip_vision\` | ~3.9GB | [HuggingFace](https://huggingface.co/h94/IP-Adapter/resolve/main/models/image_encoder/model.safetensors)，下载后重命名为此文件名 |
| `ip-adapter-plus_sdxl_vit-h.safetensors` | `ComfyUI\models\ipadapter\` | ~848MB | [HuggingFace SDXL 目录](https://huggingface.co/h94/IP-Adapter/tree/main/sdxl_models) |
| `ip-adapter-plus-face_sdxl_vit-h.safetensors` | `ComfyUI\models\ipadapter\` | ~848MB | 同上 |

> ℹ️ `ipadapter` 文件夹需要手动创建：在 `ComfyUI\models\` 目录下新建名为 `ipadapter` 的文件夹。

**2.2 ControlNet 节点 (防滑步与透视锁定)**

> **✅ 已装确认**：
> 1. 插件：画布空白处双击，搜索 `DWPreprocessor`，能搜到说明预处理器已装。
> 2. 模型：检查 `models\controlnet\` 下是否有 `diffusion_pytorch_model_promax.safetensors`。

用于锁定角色的骨架姿势（OpenPose/DWPose）或场景的边缘轮廓（Canny/Depth/Lineart），防止 AI 生成时肢体崩坏或透视走样。

*   **预处理器插件**：在 Manager 中搜索 `ComfyUI's ControlNet Auxiliary Preprocessors`（作者：Fannovel16）并安装。这个插件包含了提取骨架和线稿所需的全部算法（DWPose、Canny、Depth、Lineart 等），首次使用时会自动下载对应的小型检测模型。
*   **下载 ControlNet 模型**：

| 方案 | 模型文件 | 大小 | 下载地址 | 说明 |
|------|----------|------|----------|------|
| **推荐：统一版 (ProMax)** | `diffusion_pytorch_model_promax.safetensors` | ~6.6GB | [HuggingFace (xinsir)](https://huggingface.co/xinsir/controlnet-union-sdxl-1.0/tree/main) | 一个模型支持 12 种控制类型（OpenPose/Canny/Depth/Lineart/Scribble 等），省去下载十几个单体模型的麻烦 |
| 备选：分体版 | `diffusers_xl_canny_full.safetensors` | ~2.5GB | [HuggingFace (lllyasviel)](https://huggingface.co/lllyasviel/sd_control_collection/tree/main) | 仅 Canny 边缘检测 |
| 备选：分体版 | `diffusers_xl_depth_full.safetensors` | ~2.5GB | 同上 | 仅深度图控制 |
| 备选：分体版 | `thibaud_xl_openpose.safetensors` | ~2.5GB | 同上 | 仅骨架姿势控制 |

*   **放置路径**：将下载的模型文件放入 `{ComfyUI根目录}\ComfyUI\models\controlnet\` 目录。

> 💡 **RTX 4070 用户建议**：直接下载 ProMax 统一版即可。12GB 显存跑它完全没问题，而且一个模型就能覆盖本项目所有用到的控制类型（骨架、线稿、深度）。

### 3. 辅助工具链

**3.1 去底工具（生成透明 PNG 导入 Unity）**

> **✅ 已装确认**：在 ComfyUI 画布双击搜索 `RMBG`，能搜到 `RMBG Node` 说明已装。

废弃在线服务（如 remove.bg），全面转向 **ComfyUI 内置全自动去底**。出图即透明 PNG，零成本、无限量、不离开工作流。

*   **插件安装**：在 Manager 中搜索 `ComfyUI-RMBG`（作者：1038lab）并安装。这是一个集成了目前所有最强开源去底模型的超级节点。
*   **模型选择（节点内切换）**：
    *   **BiRefNet**：目前（2025-2026）公认的开源去底 SOTA（State of the Art）。对毛发、半透明材质（如玻璃、特效）的边缘保留极好，实测 500 张商品图毛发精度达 94%。RTX 4070 跑它只需 ~1.4 秒。
    *   **RMBG-2.0**：Bria AI 最新开源模型，性能与 BiRefNet 接近（~1.2 秒），是极佳的备选。
    *   *注：首次在节点中选择模型并运行时，ComfyUI 会自动从 HuggingFace 下载模型文件到 `models/RMBG/` 目录，无需手动下载。*
*   **像素风特化备选**：如果你的游戏是纯像素风（Pixel Art），建议在 Manager 中额外安装 `ComfyUI-TransparencyBackgroundRemover`（作者：Limbicnation）。它专门针对游戏精灵图优化，能完美保留锐利的像素边缘而不产生模糊过渡。
    *   **安装注意**：安装后，需要进入 `ComfyUI\custom_nodes\ComfyUI-TransparencyBackgroundRemover` 目录，双击运行 `install.bat` 安装 `scikit-learn` 依赖。如果不装，节点也能跑，但精度会从 100% 降到 85-90%。
    *   **节点名称**：在画布双击搜索 `TransparencyBackgroundRemover`。
    *   **像素风最佳参数**：
        *   `edge_detection_mode`: 选 `PIXEL_ART`（或 `AUTO`）
        *   `tolerance`: 调低到 `10-20`（背景颜色容差）
        *   `edge_sensitivity`: 调高到 `0.9-1.0`（保留更多边缘细节）
        *   `dither_handling`: 设为 `True`（专门处理像素风的抖动图案）
        *   `scaling_method`: 必须选 `NEAREST`（最近邻插值，防止像素变糊）
        *   `output_size`: 选 8 的倍数（如 `256x256` 或 `512x512`）

**3.2 云端炼丹炉 (LoRA 训练)**

当你在探索期积累了 30 张以上满意的同风格图片后，可以注册 [LiblibAI](https://www.liblib.art/)（国内访问友好）或 [Civitai](https://civitai.com/) 账号。使用它们的在线训练服务（Civitai SDXL LoRA 训练起步价 500 Buzz ≈ $5），上传你的图片集，训练一个专属的 Style LoRA，从而在后续量产中彻底固化画风。

### 4. 安装完成自检清单

全部安装完成后，你的文件目录应该长这样：

```
{ComfyUI根目录}\
├─ ComfyUI\
│   ├─ models\
│   │   ├─ checkpoints\
│   │   │   └─ sd_xl_base_1.0.safetensors          (~6.9GB)
│   │   ├─ clip_vision\
│   │   │   └─ CLIP-ViT-H-14-laion2B-s32B-b79K.safetensors  (~3.9GB)
│   │   ├─ controlnet\
│   │   │   └─ diffusion_pytorch_model_promax.safetensors   (~6.6GB)
│   │   ├─ ipadapter\                          ← 手动创建
│   │   │   ├─ ip-adapter-plus_sdxl_vit-h.safetensors       (~848MB)
│   │   │   └─ ip-adapter-plus-face_sdxl_vit-h.safetensors  (~848MB)
│   │   └─ loras\                              ← 后续放自己训练的 Style LoRA
│   ├─ custom_nodes\
│   │   ├─ ComfyUI-Manager\                    ← git clone 安装
│   │   ├─ ComfyUI_IPAdapter_plus\              ← Manager 一键安装
│   │   ├─ comfyui_controlnet_aux\              ← Manager 一键安装
│   │   ├─ ComfyUI-RMBG\                        ← Manager 一键安装（去底主力）
│   │   └─ ComfyUI-TransparencyBackgroundRemover\  ← 可选，像素风专用
│   └─ ...
├─ python_embeded\                         ← 自带 Python，勿动
├─ run_nvidia_gpu.bat                      ← 双击启动
└─ run_cpu.bat
```

**模型文件总占空间约 20GB**（底模 6.9 + CLIP 3.9 + ControlNet 6.6 + IPAdapter 1.7）。加上 ComfyUI 本体和插件，总共预留 **30GB** 即可开工。

### 5. 常见问题排查

| 现象 | 可能原因 | 解决方法 |
|------|----------|----------|
| 双击 `run_nvidia_gpu.bat` 闪退 | 显卡驱动过旧，不支持 CUDA 13.0 | 前往 [NVIDIA 官网](https://www.nvidia.com/download/index.aspx) 更新到最新驱动 |
| 控制台报错 `torch not compiled with CUDA` | 下载了错误的 Portable 版本 | 确认下载的是 `nvidia` 版而非 `cpu` 版 |
| 启动后浏览器白屏 | 页面未加载完成 | 等待 30 秒后刷新，或手动访问 `http://127.0.0.1:8188` |
| 右上角没有 Manager 按钮 | ComfyUI-Manager 未正确克隆 | 检查 `custom_nodes\ComfyUI-Manager\` 文件夹是否存在且非空，重启 ComfyUI |
| 跑图时报 `CUDA out of memory` | 生成分辨率过高或同时加载模型过多 | RTX 4070 建议生成分辨率不超过 1024×1024；关闭其他占用显存的程序 |
| IPAdapter 报错 `ClipVision model not found` | CLIP Vision 模型文件名不对 | 确认文件名为 `CLIP-ViT-H-14-laion2B-s32B-b79K.safetensors`（注意大小写和连字符） |
| ControlNet 出图全黑或无效果 | 选错了控制类型 | ProMax 统一版需要配合 `SetUnionControlNetType` 节点指定控制类型（如 openpose/canny/depth） |

---

## 🟢 每日开工启动（每次新开 AI 对话时必发）

> **一句话说明**：每次新开一个 Manus 对话，你需要先发一段“启动口令”让它加载项目上下文。复制下面这段话发给 Manus 就行，它会自动克隆仓库、读取进度、加载配方库，然后回复你“就绪”。

**💬 复制这段话发给 Manus：**
> GitHub Token: [填入你的Token]
> 仓库：https://github.com/jiaxuGOGOGO/MarioTrickster
> 
> 请先用 Token 克隆仓库。静默读取 `SESSION_TRACKER.md` 获取当前进度，并强制读取 `prompts/PROMPT_RECIPES.md`。
> 
> 【系统指令：加载 MarioTrickster 工业化美术管线】
> 从现在起，你是本项目的「首席技术美术(TA)」。你已知悉我本地拥有完整的 ComfyUI 环境。
> 
> 针对配方库维护与工单下发，你必须遵守【双轨架构铁律】：
> 1. 上半部（技法库）：提取教程时，按「解剖/透视/动画/光影/参数」5个知识抽屉提取通用法则，静默写入配方库。
> 2. 下半部（实体库）：当我确立新角色、新陷阱的长相时，将基础视觉 Tags 按「角色/地形/陷阱/交互物」写入实体蓝图库。
> 3. 十字交叉出图：下发工单时，必须调取【实体蓝图的基础词】叠加【知识抽屉的技法词】，并按本地节点（LoRA/Prompt/ControlNet/KSampler）物理隔离分发。绝不全揉进文本。
> 4. 冲突熔断原则：当规则冲突时，永远遵循「物理防滑步 > 空间透视正确 > 光影材质表现」的优先级。同层级知识冲突时，触发【A/B实测仲裁】与【场景分治】机制。
> 5. 拒绝小作文：对话框内仅输出可直接复制到 ComfyUI 的具体节点配置图纸。
> 
> 准备好请回复：“🛠️ 工业管线就绪。双轨架构（技法抽屉 × 实体蓝图）已加载。请下达指令。”

---

## 📚 菜单 1：喂书蒸馏 (更新上半部：通用技法抽屉)

> **一句话说明**：你找到一本好教程或设定集，丢给 Manus，它帮你把里面的干货提炼成可直接用于 ComfyUI 出图的规则，并自动存进仓库。你全程不需要写任何规则，只需要「找书 → 发书 → 等结果」。

### 角色分工速查

| 步骤 | 谁做 | 做什么 |
|------|------|--------|
| ① 找教程 | **你** | 在网上找到一本好的美术教程、设定集、或视频截图 |
| ① ½ 拆分 PDF（大书必做） | **Manus** | 收到 PDF 后，自动按章节/主题拆分为多个独立片段，并行精读，最后合并蒸馏结果；你无需手动操作 |
| ② 发给 Manus | **你** | 把教程文件上传给 Manus，并复制下方模板消息发出去 |
| ③ 蒸馏提炼 | **Manus** | 自动阅读教程，提取有用的 Tag、数值、节点约束 |
| ④ 写入配方库 | **Manus** | 自动把提炼结果写进仓库的 `prompts/PROMPT_RECIPES.md` |
| ⑤ 提交推送 | **Manus** | 自动 git commit + push，确保 GitHub 上能看到变更 |
| ⑥ 回报结果 | **Manus** | 在对话框告诉你：改了哪些文件、commit hash、是否已推送 |
| ⑦ 核对（可选） | **你** | 去 GitHub 仓库页面看一眼提交记录，确认规则已入库 |

### 💬 复制这段话发给 Manus（触发蒸馏）

> "Manus，我刚上传了一份顶级的 [美术教程/设定集]。请启动『领域精细化无损蒸馏 V4.1』：
>
> **第一阶段：拆分与并行精读**
> 0. **PDF 拆分并行精读（大书必做）**：收到文件后，先按章节/主题拆分为多个独立片段。
> 1. **强制结构化输出 (Structured Output)**：下发并行任务时，必须要求子任务输出严格的 JSON Schema（包含 `rule_name_jp`, `core_tags`, `drawer_category`, `node_target`, `conflict_check`）。禁止返回自由格式散文。
> 2. **前置去重 (Dedup at Extraction)**：子任务在提取前，必须先比对已有规则名清单（由主任务注入）。若为同义词或上位概念覆盖，直接标记 `is_duplicate: true` 并写明覆盖原因，不返回完整规则。
>
> **第二阶段：合并与仲裁**
> 3. 判断核心价值，严格归入 5 个「知识抽屉」的 1-2 个：🧑‍♂️[解剖与形态] / 📐[透视与物件] / 🏃[动画与物理] / 🎨[光影与材质] / ⚙️[AI 硬核参数]。
> 4. **冲突仲裁**：如果新知识与抽屉内现有规则矛盾：
>    - 若跨优先级，高优先级（如防滑步）直接秒杀低优先级。
>    - 若同优先级，执行**场景分治**。
>    - 若无法分治，派发**【A/B实测工单】**。
>
> **第三阶段：锡点写入与闭环**
> 5. 只提炼能生效的 Tag、数值和节点约束。绝不写散文。
> 6. **锡点写入 (Anchor-based Insert)**：将提炼的规则通过 `<!-- INSERT_XXX_RULES_HERE -->` 锡点标记直接注入 `prompts/PROMPT_RECIPES.md`，禁止使用行号定位。
> 7. **强制 GitHub 闭环**：完成蒸馏后，执行 `git diff`，更新 `SESSION_TRACKER.md`，提交 commit 并 push。
> 8. 回报：`已修改文件列表`、`commit hash`、`是否已推送到 GitHub`。如果只有附件、没有仓库提交与推送，视为**任务未完成**。"

### 菜单 1：GitHub 闭环验收（强制）

| 阶段 | 必做动作 | 验收信号 |
|------|----------|----------|
| **A. 落库** | 在仓库工作区内直接修改 `prompts/PROMPT_RECIPES.md` | `git diff -- prompts/PROMPT_RECIPES.md` 能看到差异 |
| **B. 记账** | 同步更新 `SESSION_TRACKER.md` 的 **最新 Session / 回归验证说明 / 待办队列** | 会话记录能解释这次蒸馏改了什么 |
| **C. 清洁** | 临时 OCR、摘录、草稿只作过程文件；不把它们当成主成果提交 | 工作区不会被临时文件淹没 |
| **D. 入库** | `git add` → `git commit` → `git push origin master` | GitHub 仓库页面可见最新提交 |
| **E. 回报** | 回复中给出 **修改文件 + commit hash + 推送结果** | 用户可直接去 GitHub 核对 |

> **一句话判定**：`附件 ≠ GitHub 变更`。只有**仓库文件已改、commit 已生成、远端已推送**，菜单 1 才算真正完成。

> **推荐提交信息模板**：`docs(art): close distillation-to-github loop and sync prompt recipes`

> **推荐结果回报模板**：`已落库：PROMPT_RECIPES.md / SESSION_TRACKER.md / ART_PIPELINE_GUIDE.md；已提交：<commit_hash>；已推送：GitHub 可见。`

---

## 👾 菜单 2：新增设定/确立画风 (更新下半部：实体蓝图库)

> **一句话说明**：你想出了一个新怪物或新物件的长相，告诉 Manus，它帮你把视觉特征录入蓝图库，以后每次出图都会自动调用。或者你想确立全局画风，Manus 会给你发抽卡工单，你在本地 ComfyUI 跑图。

### 角色分工速查

**情况 A：加新怪/新物件**

| 步骤 | 谁做 | 做什么 |
|------|------|--------|
| ① 想好新实体 | **你** | 想好新怪物/新陷阱/新物件的大致长相（用自然语言描述即可） |
| ② 告诉 Manus | **你** | 复制下方模板，填入你的描述，发给 Manus |
| ③ 提炼入库 | **Manus** | 自动提炼视觉特征 Tags，写入 `PROMPT_RECIPES.md` 下半部对应分类 |
| ④ 提交推送 | **Manus** | 自动 git commit + push |

**情况 B：确立全局画风**

| 步骤 | 谁做 | 做什么 |
|------|------|--------|
| ① 上传参考图 | **你** | 找到你喜欢的画风参考图，上传给 Manus |
| ② 发模板 | **你** | 复制下方情况 B 模板发给 Manus |
| ③ 下发工单 | **Manus** | 调取光影与参数抽屉，给你发一份 ComfyUI 节点配置图纸 |
| ④ 本地抽卡 | **你** | 按图纸在 ComfyUI 里跑图，抽够 30 张满意的 |
| ⑤ 炼 LoRA | **你** | 用 30 张图去 LiblibAI/Civitai 训练专属 Style LoRA |

### 💬 复制这段话发给 Manus

> **情况 A (加新怪/新物件)**："Manus，我们游戏里新增了一个资产设定：**【👉比如：一种带毒的飞行眼球怪】**。请提炼它最核心的视觉特征 Tags，归类到 `prompts/PROMPT_RECIPES.md` 下半部对应的实体分类（角色/地形/陷阱/交互物）中。以后做它必须调用此蓝图。"
>
> **情况 B (找参考图抽卡炼全局画风 LoRA)**："Manus，我要确立基准画风。我上传了参考图。请调取[光影]与[AI参数]抽屉，给我出卡工单。我会在本地抽够 30 张去炼全局 Style LoRA。"

### 2.1 菜单2改成“单一固定入口”

上一版把菜单2拆成“测试模式 / 生产模式”双入口，虽然比更早的“补位”“换槽位”写法清楚，但用户仍然要先判断自己该走哪一支，心智成本还是偏高。这里继续收敛：**菜单2以后只保留一个固定入口**。你不再需要先报“测试模式”还是“生产模式”；你只需要在同一句里说明**这轮目标强度**，由 Manus 自动决定给你轻量验证工单还是正式生产工单。

| 这轮目标 | 适合什么时候说 | Manus 应做什么 | 你的收益 |
|------|------|------|------|
| **先验证方向** | 你现在只是想先跑通流程、验证节点、看风格是否可行 | 给出最小可执行工单，优先保证流程通、方向对、结构不崩 | 用最低沟通成本确认方向 |
| **进入项目基线并支持后续量产** | 你准备把结果纳入项目资产库，要求后续可持续复用 | 给出正式生产工单，明确 LoRA、ControlNet、参数安全区间与返工标准 | 保证资产质量、统一性与复用性 |

> **记住一个最简单的原则：**
>
> 菜单2不是让你一次说完所有信息，而是让你用**同一个固定口令**先开局，再由 Manus 只追问真正会卡住执行或明显影响质量的关键缺项。不会阻塞执行的内容，先不补。

### 💬 菜单2固定口令（统一版）

> **"Manus，我现在走菜单2。我要做的是【角色 / 陷阱 / 交互物 / 场景】中的【填写项】。我这边【有 / 没有】参考图；如果有，我会上传参考图、现有素材或风格说明。这一轮的目标是【先验证方向 / 进入项目基线并支持后续量产】。请你据此给我下发对应强度的工单；如果信息不够，只追问那些会阻塞执行或明显影响结果质量的必要项。"**

### 菜单2里你只需要额外补这三类信息

| 信息类型 | 什么时候必须补 | 例子 |
|------|------|------|
| **你要做什么** | 永远都要说 | 角色、陷阱、交互物、场景 |
| **你手上有什么参考** | 有就给，没有就跳过 | 参考图、现有素材、期望风格词 |
| **你这轮想达到什么强度** | 永远都要说 | 先验证方向 / 进入项目基线并支持后续量产 |

> 其余像镜头、材质、动作、LoRA、最终 Prompt，不要求你在菜单2开局时一次说满。Manus 只在这些内容真的影响当前执行时才追问。

### 菜单2最短可复制版

| 场景 | 极简短句 |
|------|------|
| **菜单2统一入口** | `我现在走菜单2，要做【对象】；【有 / 没有】参考。这轮目标是【先验证方向 / 进入项目基线】。请按对应强度下工单，只追问关键缺项。` |

> **实际使用建议**：如果你懒得组织语言，就先发上面的极简短句；如果你已经知道对象和参考情况，直接用完整版口令效率更高。两种发法都合法，不需要额外背更多规则。

### 菜单2最少记住的原则

| 原则 | 含义 |
|------|------|
| **先说做什么** | 角色、陷阱、交互物、场景至少要说清一类 |
| **再说这轮目标强度** | 明确是先验证方向，还是直接进入项目基线 |
| **有参考就顺手给** | 没有也可以先开工，不必卡住 |
| **缺信息只补关键项** | 不需要一开始把镜头、材质、动作全部讲完 |

> 这套写法的目的不是让你多说，而是让你每次只补最关键的信息，就能让 Manus 稳定接住并继续往下推进。

---

### 2.2 菜单2与你的真实操作关系

| 你当前状态 | 你怎么发最合适 |
|------|------|
| **只是先开口，不想组织太多句子** | 直接发“菜单2极简短句” |
| **已经知道要做什么且手上有参考** | 直接发“菜单2完整版口令”，把对象、参考和目标强度揉进一句话 |
| **当前只是想低成本试方向** | 在同一句里明确写“这轮先验证方向” |
| **当前已经进入正式资产生产** | 在同一句里明确写“进入项目基线并支持后续量产”，避免后面返工时基准不稳 |

> **一句话结论**：第二层那张“三类信息表”不是让你每次背诵，而是提醒你——最好把这三类信息尽量揉进菜单2那一句开场里。这样最省来回确认，也最不容易误解。

---

### 2.3 菜单2推荐发法（最稳版）

> **统一推荐发法**：
>
> **"Manus，我现在走菜单2。我要做的是【填写对象】。我这边【有 / 没有】参考图；如果有，我会上传参考图、现有素材或风格说明。这一轮的目标是【先验证方向 / 进入项目基线并支持后续量产】。请你据此给我下发对应强度的工单；如果信息不够，只追问会阻塞执行或明显影响结果质量的必要项。"**

> 你以后可以只改【对象】、【有/没有参考】和【这轮目标】这三处，其他句子长期固定使用。这样既保留前期验证弹性，也能守住正式生产的质量基准。
>
> **补充规则（给后续 LoRA 训练留测试覆盖面时）**：`Sample Media Prompts` **默认先按 3 类**组织，优先覆盖 **角色 + 场景/地块 + 敌人/机关** 这组三大代表类；只有当你明确说这轮要更接近真实生产覆盖时，再扩到 **4–5 类**；**一般不要超过 5 类**，也不要重复覆盖同一资产类。

---

## 🌉 菜单 2.5：过渡期（炼丹与管线换轨）

> **一句话说明**：当你在菜单2抽够了 30 张满意的画风图后，必须经过这个过渡期，将画风固化为 LoRA，并废弃 IPAdapter 垫图流程，为菜单3的极简量产做准备。

### 1. 预算规划与需求定调
对于当前的 MarioTrickster 项目，**绝不需要为每个怪物或陷阱单独训练 LoRA**。所有的角色、地形、陷阱、交互物都共享一套底层视觉规则。
- **核心需求**：只需训练 1 个全局画风 LoRA (`trickster_style`)，即可覆盖当前所有 22 个实体蓝图。
- **成本预算**：使用 Civitai 线上训练，单次成本 500 Buzz。包含调参迭代，预估总消耗 1500-2000 Buzz，完全在 ¥88/月（10000 Buzz）的预算安全线内。

### 2. Civitai 训练实战参数（防浪费指南）
请将菜单2跑出的 30 张画风图压缩为 `.zip`（**不需要打标签**）。在 Civitai 发起训练时，严格遵守以下参数以确保一次成功：

| 参数名 | 推荐设定值 | 设定理由 |
| :--- | :--- | :--- |
| **Model Type** | `Style` | 极其重要！选 Character 会导致模型过度拟合特定角色，导致地形出图崩坏。 |
| **Base Model** | `SDXL 1.0 (Distilled/Predictive) - FP32-SFW` | ⚠️ **必须选这个！** 绝对不要选 CUSTOM，否则成本翻倍（1000 Buzz 起步）且极易失败。 |
| **Resolution** | `1024` | ⚠️ **必须改！** 默认可能是 512，但 SDXL 必须用 1024，否则细节全丢。 |
| **Sample Media Prompts** | 手动填入测试词 | ⚠️ **必须改！** 删掉自动生成的风景词。**默认先写 3 类**，用来覆盖最小测试集；如果这轮明确要覆盖更接近真实生产的资产面，再扩到 **4–5 类**；**一般不要超过 5 类**。推荐默认三条：`Image #1: trickster_style, Mario-like character, running pose`<br>`Image #2: trickster_style, underground cave tileset`<br>`Image #3: trickster_style, green mushroom enemy` |
| **Epochs** | `10` | Style LoRA 需要多轮次才能吃透画风。 |
| **Network Dim (Rank)** | `64` | 像素艺术需要足够的维度来记忆边缘硬度和色彩块面。 |
| **Network Alpha** | `32` | 保持为 Dim 的一半，防止权重爆炸。 |
| **Optimizer** | `Prodigy` | 选 Prodigy 后，下面的 Unet LR 和 Text Encoder LR 保持 `1` 即可（它会自动调节）。 |
| **Shuffle Tags** | `勾选` | 增强泛化能力，让 AI 理解特征组合。 |
| **Keep Tokens** | `1` | 确保触发词 `trickster_style` 始终在最前面不被洗牌打乱。 |

> **数量判定原则**：三条不是硬上限，而是默认最小测试集。测试阶段优先用 3 类快速看 LoRA 有没有学到项目主风格；只有当你明确需要扩大覆盖面时，才加到 4–5 类。

*注：训练完成后，请下载最后三个 Epoch（如 Epoch 8, 9, 10）的 `.safetensors` 文件，放入本地 ComfyUI 的 `models/loras/` 目录下。*

### 3. 管线换轨：废弃垫图，极简重构
当你拿到 LoRA 文件后，管线将发生质变。在进入菜单3前，我们必须进行以下重构：
1. **废弃 IPAdapter**：基于社区共识与实测，IPAdapter 每次出图都要重新计算 CLIP 嵌入，这种概率性会导致连续动画帧出现严重的**特征漂移**和**闪烁**。因此，在量产期必须彻底删除菜单2中臃肿的垫图节点，节约算力并保证绝对一致性。
2. **极简双轨架构**：改用 `Base Model -> Load LoRA -> Text Prompt` 的物理隔离架构。LoRA 已经将画风“烧入”了模型权重，能提供 IPAdapter 无法比拟的确定性。
3. **Prompt 减负**：删除所有 `pixel art, cel shading, flat colors` 等画风词，只保留 `[LoRA 触发词] + [实体蓝图词] + [技法抽屉词]`。
4. **提示词接力棒必须拆成四层理解**：菜单2.5和菜单3里出现的“提示词”不是同一个东西，不能混用；同时，**菜单2建立的目标、参考、约束要无损传递到菜单3**，不应在换轨时丢失。

| 层级 | 出现位置 | 谁填写 | 作用 | 是否直接喂给最终生图 |
|------|----------|--------|------|----------------------|
| **A. 训练测试词** | Civitai 的 `Sample Media Prompts` | **你** | 只用于训练平台在每个 Epoch 抽样预览 LoRA 是否学到画风 | **否** |
| **B. 换轨登记口令** | 发给 Manus 的一句话 | **你** | 告诉 Manus：LoRA 文件已落地，并要求把菜单2既有目标、参考与约束无损继承到菜单3 | **否** |
| **C. 菜单3资产需求口令** | 菜单3的需求模板 | **你** | 描述这次具体要做什么资产、静态还是动画、是否要透视/动作约束 | **否** |
| **D. 最终生图 Prompt** | Manus 下发工单里的 `Text Prompt` 节点 | **Manus** | 把 `LoRA 触发词 + 实体蓝图词 + 技法抽屉词 + 反向防错词` 组装成真正喂给模型的极简 Tags | **是** |

> **记忆口诀**：`菜单2.5 的训练测试词 ≠ 发给 Manus 的换轨登记口令 ≠ 菜单3的资产需求口令 ≠ 最终生图 Prompt`。
>
> 真正进入出图节点的，永远只有最后一层 **D**。前面三层都只是“管线控制语”；其中 **B** 的职责不是重说一遍需求，而是把前序上下文**无损接棒**到菜单3。

### 3.1 菜单2.5 → 菜单3 的接棒公式（统一版）

菜单2.5 到菜单3不需要你记“换点表”、也不需要你判断“这是测试还是正式”。**这里也只保留一个固定入口。** 只要 LoRA 已训练完成，无论你是先做验证图，还是已经进入正式量产，都走同一句接棒口令；差别只体现在 Manus 下发给你的菜单3工单强度，而不是你要再背第二套说法。

| 阶段 | 你负责补什么新增信息 | Manus 必须无损继承什么 | 最终结果 |
|------|------|------|------|
| **菜单2抽卡期** | 当前要做什么、是否有参考、这轮目标强度 | 当前需求目标、参考、风格方向、约束条件 | 先拿到满意风格样本 |
| **菜单2.5炼 LoRA** | `LoRA 文件名 + 触发词` | 菜单2里已经确认的对象、参考、目标与约束 | 产出全局 Style LoRA |
| **菜单3继续执行** | 本轮新增资产需求（如有） | 已登记的 LoRA 底座 + 前序上下文 + 本轮新增需求 | 用同一底座继续完整流程 |

> **一句话理解**：菜单2.5负责把画风固化成 LoRA；菜单3负责在**不丢前文信息**的前提下继续执行。中间不是重新开一个新任务，而是完成一次**LoRA 登记并无损续跑**。

### 💬 菜单2.5 → 菜单3 固定接棒口令（统一版）

> **"Manus，菜单2.5已经完成。当前可用的 LoRA 信息如下：文件名是 `【填写文件名】`，触发词是 `【填写触发词】`。请你把它登记为当前菜单3默认底座，并在不丢失菜单2既有目标、参考与约束的前提下，继续按菜单3完整流程为我下发本轮工单；如果还缺会阻塞执行或明显影响结果质量的必要项，再单独指出。"**

### 菜单2.5 → 菜单3 的最少原则

| 原则 | 含义 |
|------|------|
| **只补新增事实** | 你在这里通常只补 `LoRA 文件名 + 触发词` 两项即可 |
| **前文信息无损继承** | 菜单2已经说过的目标、参考、约束，不应在这里要求你重报一遍 |
| **测试也走完整流程** | 即便这轮只是先验证方向，只要 LoRA 已落地，也照样走这条统一入口，再由 Manus 给轻量版菜单3工单 |
| **正式生产也走同一句** | 若你已进入量产，不换口令，只提高菜单3工单规格 |

> **最少原则**：菜单2.5 到菜单3不是重开新单，而是把 LoRA 底座接进原流程。你通常不需要在这里重新讲一遍完整资产需求，除非这轮有新的对象或新的约束要补充。

### 示例（仅演示填法，不是第二个入口）

> **"Manus，菜单2.5已经完成。当前可用的 LoRA 信息如下：文件名是 `trickster_style_e10.safetensors`，触发词是 `trickster_style`。请你把它登记为当前菜单3默认底座，并在不丢失菜单2既有目标、参考与约束的前提下，继续按菜单3完整流程为我下发本轮工单；如果还缺会阻塞执行或明显影响结果质量的必要项，再单独指出。"**

---

## 🏭 菜单 3：批量生产资产 (量产期：实体蓝图 × 知识抽屉 = 节点发牌)

> **一句话说明**：你告诉 Manus 你要做什么资产（比如"地刺的静态图"），Manus 自动从配方库拼装好所有节点参数，给你发一份可直接复制到 ComfyUI 的工单。你只需要「说需求 → 收工单 → 本地跑图 → 交回结果」。
>
> **⚠️ 工单质量标准**：所有工单必须遵守 `docs/WORKORDER_QA_STANDARD.md` 中定义的格式规范（节点中文搜索名 + 默认值→目标值 + 端口对端口连线表 + 具体数值故障排除）。不合规的工单视为未完成。

### 角色分工速查

| 步骤 | 谁做 | 做什么 |
|------|------|--------|
| ① 提需求 | **你** | 告诉 Manus 你要做什么资产（复制下方模板，填入具体需求） |
| ② 拼装工单 | **Manus** | 自动从配方库抽取实体蓝图 + 技法规则，组装成 ComfyUI 节点配置图纸 |
| ③ 本地跑图 | **你** | 一字不差按图纸配置 ComfyUI，点跑图 |
| ④ 图坏了？甩回去 | **你** | 图跑出来不对，**绝不自己调参数**，截图发给 Manus 说“哪里不对” |
| ⑤ 重新发牌 | **Manus** | 根据坏图现象调整策略，重新发工单 |
| ⑥ 满意→切图入库 | **你** | 去底→拖进 Unity→跑测试验收 |

### 💬 复制这段话发给 Manus（触发工单）

> "Manus，我要做游戏资产：**【👉 需求：例如，[交互物] 木制宝箱开启的 3 帧动画 / 或 [陷阱] 静态地刺】**。
> 这段话只是**资产需求口令**，不要把我的整段中文需求原样塞进最终生图 Prompt。
> 请启动『十字双轨组装』流程，查阅 `prompts/PROMPT_RECIPES.md`：
> 
> 0. **接棒确认**：先告诉我你当前量产底座使用的是哪个 `Base Model`、哪个 `LoRA 文件名`、哪个 `LoRA 触发词`。
> 1. **双轨抓药声明**：明确告诉我你本次提取了下半部的哪个【实体蓝图】（长什么样）？并且拉开了上半部的哪几个【知识抽屉】（怎么画）？(注：比如做静态陷阱严禁混入动画规则，遇冲突执行熔断)。
> 2. **输出「节点化」制造工单**：请给我一份可直接在 ComfyUI 本地照抄的 Markdown 工单。严格按以下节点分类输出：
>    - 🖼️ **[Load LoRA 节点]**：项目专属 LoRA 的名称、触发词及推荐权重。
>    - 📝 **[Text Prompt 节点]**：只写最终会实际喂给模型的极简 Tags（`LoRA触发词 + 实体蓝图词 + 技法抽屉词 + 反向防错词`），要求 <20 词；**禁止**把我的整段需求句原样复制进去。
>    - 🤖 **[ControlNet 节点]**：根据 🏃[动画] 或 📐[透视] 抽屉的约束，明确告诉我该启用哪个模型（OpenPose/Canny/Depth），以及我该准备什么样的参考底图喂给它防崩坏。
>    - ⚙️ **[KSampler 节点]**：根据 ⚙️[AI参数] 抽屉，给出建议的 CFG 和 Denoising 安全数值。
> 绝对不要在对话框写小作文，只给我可以直接 Copy 的节点图纸！"

### 3.2 菜单 3 的最优分流：不要再让一个万能口令硬吃所有资产

在量产期，**最优方案不是继续使用一个万能需求模板**，而是先按资产的结构约束类型分流，再触发对应工单。这样可以同时提高出图稳定性与返工效率。

| 分支口令 | 最适合的任务 | 必拉抽屉 | 默认结构约束 | 典型失败风险 |
|------|------|------|------|------|
| **静态资产版** | 地刺、火焰喷口、砖块、宝箱、单帧交互物、小型地形块 | 光影 / 参数；必要时补透视 | 非生物默认 `Lineart + Canny`；简单单体可允许空 ControlNet | 轮廓软塌、材质串味、把静态物画出动作感 |
| **动画序列版** | 主角跑步、跳跃、攻击；怪物巡逻；交互物开合多帧 | 动画 / 解剖 / 参数 | 生物强制 `DWPose/OpenPose`；硬表面运动件用 `Lineart + Canny`；建议 `Img2Img` | 滑步、骨折、帧间漂移、动作重心错位 |
| **透视场景版** | 平台关卡段、室内房间、带前后景层级的假游戏截图、复杂机关区 | 透视 / 光影 / 参数 | `Depth` 优先，必要时叠 `Canny` 锁结构 | 空间塌陷、地面倾斜、平台高度错乱、遮挡关系错误 |

> **分流原则**：先判断这次资产最怕坏在哪里，再选模板。
>
> 如果最怕 **滑步和帧间漂移**，走 **动画序列版**；如果最怕 **空间结构崩塌**，走 **透视场景版**；如果只是做 **单个可量产素材**，走 **静态资产版**。

### 💬 复制这段话发给 Manus（静态资产版）

> "Manus，我要做 **菜单3-静态资产版** 游戏资产：**【👉 需求：填写具体静态资产，例如 [陷阱] 静态地刺 / [交互物] 关闭状态宝箱 / [地形] 断裂砖块】**。
> 请按『静态资产分流』处理，不要误拉动画抽屉。
> 先执行接棒确认，然后从 `prompts/PROMPT_RECIPES.md` 中抽取最匹配的【实体蓝图】与【光影/参数】抽屉；如果资产涉及明确空间朝向，再补【透视】抽屉。
> 请输出可直接复制的 ComfyUI 工单，并严格分成四区：
> - 🖼️ **[Load LoRA 节点]**：LoRA 文件名、触发词、权重。
> - 📝 **[Text Prompt 节点]**：只写 `LoRA触发词 + 实体蓝图词 + 光影抽屉词 + 反向防错词`；若无必要，禁止混入动画词。
> - 🤖 **[ControlNet 节点]**：默认按非生物硬表面处理；告诉我是否启用 `Lineart / Canny`，以及底图需要是纯轮廓图、线稿图还是可空。
> - ⚙️ **[KSampler 节点]**：给出最稳的静态资产参数，不要为了速度牺牲边缘稳定性。
> 只给节点图纸，不写解释。"

### 💬 复制这段话发给 Manus（动画序列版）

> "Manus，我要做 **菜单3-动画序列版** 游戏资产：**【👉 需求：填写具体动画，例如 [角色] 主角跑动 8 帧 / [角色] 跳跃三阶段 / [交互物] 宝箱开启 3 帧】**。
> 请按『动画序列分流』处理，强制优先遵守 **物理防滑步 > 空间透视正确 > 光影材质表现**，严禁把静态单帧策略直接套到序列帧。
> 先执行接棒确认，然后从 `prompts/PROMPT_RECIPES.md` 中抽取最匹配的【实体蓝图】与【动画/解剖/参数】抽屉；若镜头有明显角度变化，再补【透视】抽屉。
> 请输出可直接复制的 ComfyUI 工单，并严格分成四区：
> - 🖼️ **[Load LoRA 节点]**：LoRA 文件名、触发词、权重。
> - 📝 **[Text Prompt 节点]**：只写 `LoRA触发词 + 实体蓝图词 + 动画抽屉词 + 解剖抽屉词 + 反向防错词`，要求极简。
> - 🤖 **[ControlNet 节点]**：如果是生物关节角色，强制指定 `DWPose/OpenPose`；如果是机械或硬表面运动件，说明是否改用 `Lineart + Canny`；同时告诉我每一帧参考底图该如何准备，避免骨折与漂移。
> - ⚙️ **[KSampler 节点]**：给出最稳的序列参数；若使用 Img2Img，请明确 `Denoising Strength` 安全区间，优先保证帧间一致性而不是单帧花哨。
> 只给节点图纸，不写解释。"

### 💬 复制这段话发给 Manus（透视场景版）

> "Manus，我要做 **菜单3-透视场景版** 游戏资产：**【👉 需求：填写具体场景，例如 [地形] 地下城平台区透视场景 / [场景] 带前景立柱的机关房间 / [关卡] 可横版游玩的假截图】**。
> 请按『透视场景分流』处理，优先解决地面水平线、平台厚度、前后景遮挡和可落脚空间正确性，不要误用角色动画抽屉。
> 先执行接棒确认，然后从 `prompts/PROMPT_RECIPES.md` 中抽取最匹配的【实体蓝图】与【透视/光影/参数】抽屉；只有场景内存在角色动作约束时，才额外补【动画/解剖】抽屉。
> 请输出可直接复制的 ComfyUI 工单，并严格分成四区：
> - 🖼️ **[Load LoRA 节点]**：LoRA 文件名、触发词、权重。
> - 📝 **[Text Prompt 节点]**：只写 `LoRA触发词 + 实体蓝图词 + 透视抽屉词 + 光影抽屉词 + 反向防错词`，禁止把整段场景描述原样塞进去。
> - 🤖 **[ControlNet 节点]**：优先说明是否启用 `Depth`，是否需要叠加 `Canny` 锁硬边结构，并明确我要准备哪种场景草图或深度底图。
> - ⚙️ **[KSampler 节点]**：给出最稳的场景参数，优先保证空间结构清晰与边缘可切图性。
> 只给节点图纸，不写解释。"

### 3.3 如何在三套模板里做选择

| 你的任务长相 | 直接选哪套 | 原因 |
|------|------|------|
| 单个陷阱、单个砖块、单个宝箱、单帧交互物 | **静态资产版** | 目标是快速稳定量产，没必要引入动画约束 |
| 主角动作、怪物动作、开关开合、多帧循环 | **动画序列版** | 核心问题是帧间一致性与防滑步 |
| 一个完整平台区、房间、假游戏截图、复杂机关空间 | **透视场景版** | 核心问题是空间结构而不是单体造型 |
| 既有角色动作又有强透视场景 | **先走动画序列版，再触发透视补强工单** | 冲突时优先满足物理防滑步，再补空间透视 |

> **实战铁律**：不要一上来就追求“一个工单同时搞定角色动作 + 大场景透视 + 材质细节”。
>
> 最优效率方案是 **先保运动正确，再保空间正确，最后补材质表现**。这样返工成本最低，也最符合当前双轨架构的熔断优先级。

### 3.4 菜单3返工规则：默认给我“修正版工单”，必要时附变更摘要

上一版把返工流程写成“只改哪一格”，适合精修，但不适合作为默认协作方式。现在统一改成更稳的原则：**当菜单3结果不对时，你优先让 Manus 直接重发一份完整可复制的修正版工单；只有当你明确说想节省改动量时，它才额外附一份简短变更摘要。** 这样测试时省心，生产时也不会因为漏改一两个节点而污染基准。

| 使用场景 | 你应该怎么做 | Manus 应怎么回 |
|------|------|------|
| **测试阶段返工** | 直接描述现象 | 默认返回一份完整修正版工单，方便你整份照抄 |
| **生产阶段返工** | 描述现象，并说明要保持当前量产基座 | 返回完整修正版工单，并附最多 3 项关键改动摘要 |
| **你只想局部微调** | 明确说“只告诉我最小改动” | 再改为输出最小变更说明 |

### 💬 菜单3返工固定口令（默认版）

> **"Manus，上一版菜单3工单需要返工。请你不要只做零散补丁，而是基于当前问题重新整理并重发一份完整修正版工单，保证我可以直接替换旧版本继续执行。本次需要修正的核心问题是：【填写问题，例如风格偏差 / 结构不稳 / 提示词过散 / LoRA 调用不准】。如果你判断某些问题只改局部会导致上下文继续冲突，请直接一并收敛进完整新版本。"**

### 💬 菜单3返工固定口令（最小改动版）

> **"Manus，菜单3反馈。资产是【你的资产名】；当前问题是【你的现象描述】。这次不要整份重发，只告诉我最小必要改动。"**

### 菜单3返工最短可复制版

| 场景 | 极简短句 |
|------|------|
| **默认整单重发** | `上一版菜单3需要返工，请直接重发一份完整修正版工单，问题是【X】。` |
| **只要最小改动** | `上一版菜单3需要返工，这次不要重发整份，只告诉我最小必要改动，问题是【X】。` |

> **返工默认原则**：默认优先整单重发，因为这最不容易漏改，也最适合测试与正式生产共用；只有你明确说“只要最小改动”时，才改用局部修正模式。

### 🛠️ 你拿到工单后该怎么做

| 步骤 | 谁做 | 做什么 |
|------|------|--------|
| ① 纯粹执行 | **你** | 一字不差按 Manus 给的节点图纸配置 ComfyUI |
| ② 遇错甩锅 | **你** | 图跑出来透视崩了或肢体错了，**绝不自己调参数**！带上坏图告诉 Manus："工单出图失败，现象是XXX，请调整 ControlNet 策略或按熔断原则重新发图纸！" |
| ③ 切图入库 | **你** | 满意后去底，拖进 Unity 点 `一键工业化切图`，跑测试验收 |

---

## 🌟 为什么加上这个最终拼图后，管线真正做到了"天下无敌"？

在这个最终体系下，你的 GitHub 仓库就像一个会**"自动复利"**的超级大脑：

1. **无限扩展不乱套**：明天你要加个新怪物"食人花"，Manus 只会在【实体库-角色】里记下"绿色植物、长满獠牙"。但它出图时，会自动继承之前你喂给它的所有透视和光影抽屉里的顶级知识！
2. **全军自动升级**：后天你喂给 Manus 一本《金属材质渲染指南》，它更新了【知识抽屉-光影】。结果就是，你所有的宝箱、铁刺陷阱、主角盔甲，在下一次出图时，自动全军升级了完美的金属质感！

**实体设定（数据）与 绘画规则（算法）彻底解耦，互相复用，绝不串味。这就是真正的 3A 级美术工业流水线！**

*Last Updated: 2026-04-11 V4.4 by Manus TA*
