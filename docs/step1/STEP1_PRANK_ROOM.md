# 第 1 步：恶作剧房间（一间手工房 + 冲冲型马里奥）

> 依据：`docs/DESIGN_CONSTITUTION_v1.0.md` 开发顺序第 1 步。
> 退出条件：**连玩 20 局仍想玩** 且 **至少 3 种不同的坑法**。AI 不判断"好不好玩"，只记录。

## 1. 怎么打开

1. Unity 菜单 `MarioTrickster → Step 1 → Build Prank Room`（第一次会生成场景和调参资产）。
2. 以后直接 `MarioTrickster → Step 1 → Open Prank Room`。
3. 按 Play。马里奥开局站 2 秒（给你就位），然后自己去拿右边的宝物、再跑回左边的出口。

生成物：
- 场景 `Assets/Scenes/Step1_PrankRoom.unity`
- 调参 `Assets/Resources/Step1/RushMarioTuning.asset`（所有数字都在这里改，不用改代码）

## 2. 你（捣蛋者）的按键

| 按键 | 作用 |
|------|------|
| 方向键 | 移动；↑ / 右 Ctrl / 小键盘 0 跳 |
| P | 伪装成道具（要贴着机关） |
| O / I | 切换伪装外观 |
| L | 触发身边的机关（需要已伪装、完全融入） |
| C | 切换镜头：整屏 → 两人同框 → 跟自己 |
| Esc / R / N | 暂停 / 重开关卡 / 下一回合 |
| 1–5 | 回合结束时打分："还想再玩一局吗"（1=不想，5=马上再来） |

## 3. 规则

- 你有 **3 条命**。被马里奥**看见本体并贴身**抓到 = 掉 1 条命、回出生点、2 秒无敌。3 条命用完 → 马里奥赢。
- 马里奥**拿到宝物并跑回出口** → 马里奥赢。
- 马里奥被机关打掉 3 滴血，或 150 秒计时结束 → **你赢（过关）**。
- 计分放到第 3 步，这一步不算分。

## 4. 马里奥"脑子里在想什么"（头顶标记）

| 标记 | 意思 | 他会做什么 |
|------|------|-----------|
| （无） GET LOOT / RUN HOME | 没起疑 | 直冲目标 |
| **?** 黄色 | 有点怪 | 停下来朝那边看一眼 |
| **!** 橙色 | 确定有问题 | 走过去，到跟前就扫描（扫描结果是真的） |
| **!!** 红色 | 看见你本体了 | 追你 |
| **?!** | 追丢了 | 去最后看见你的地方找，找不到就回去干正事 |
| OUCH! / GOTCHA! | 被机关打 / 抓到你 | — |

头顶白/黄/红条 = 起疑程度。脚下的扇形线 = 他的**视野**（被墙挡住的部分会截断，单向台面不挡）。

**公平规则（宪法 H2/H4）**：
- 他**永远先显示 "?" 至少 0.4 秒**，才可能变成 "!"——你总有反应时间。
- 他只知道**眼睛看到的东西**：视野扇形 + 距离 9 + 无遮挡。背后、墙后、视野外的事他不知道。
- 伪装后**站着不动**他不会怀疑；伪装着**移动**被他看见才会起疑。
- 他**亲眼看见**机关突然启动会起疑（没看见就不会）；被机关打到也会起疑。

## 5. 房间里的坑法（至少试出 3 种）

```
W..................................W
W..................................W
W..................................W
W..................................W
W.................T................W   T = 你的出生点（台子上）
W.....---........----....----......W   --- = 单向台面（可站、可从下面跳上去、不挡视线）
W.G.M....~..........~[.....~....o..W   G 出口 / M 马里奥 / ~ 火焰 / [ 封路机关 / o 宝物
W###########CCCC-##################W   CCCC = 崩塌桥（踩上 1 秒塌，5 秒后复原）
W###########.~...##################W   坑里还有一个火焰
W##################################W
```

| 坑法 | 怎么做 | HUD 记为 |
|------|--------|----------|
| 火焰 | 伪装在火焰旁，马里奥路过时 L 触发（预热闪烁是给他的预警） | Fire |
| 封路拖延 + 火焰 | 触发 `[` 让墙挡住他，他停在旁边火焰上 | Blocker / Fire |
| 桥塌掉坑 | 他过桥时触发崩塌，掉进坑里再点坑里的火 | Collapse / Fire |
| 引诱脱身 | 故意让他看见你，被追后躲进视野外/伪装，让他 ?! 后放弃 | Escape |

**火焰平时是安全的**：本房间的火焰只有你触发才会喷（`coolOffDuration` 被设成极大）。所以不碰键盘时，马里奥应该能自己通关——这就是宪法 H10 的检查。

## 6. 为什么镜头不再跟着马里奥

以前镜头跟马里奥，你（捣蛋者）经常在画面外，看不到自己。第 1 步把房间做成**一屏大小**，默认**整屏镜头**（《地狱邻居》式）：你和马里奥始终都在画面里，一眼能看到他的视野扇形和头顶标记，方便你判断"现在动会不会被看见"。
如果想试别的：按 **C** 切到"两人同框"或"跟着自己"。默认模式可以在调参资产里改（`cameraMode`）。

## 7. 必做检查

1. **EditMode 测试**：Test Runner 跑 `Step1RushMarioTests`（16 项），以及 `H4PerceptionHonestyTests` / `Step0CoreLoopTests` 保持全绿。
2. **H10（无干扰通关）**：Play 后**不碰任何键**，看马里奥能否自己拿宝回家。建议连看 5 局；如果有 1 局卡住，记下卡在哪（位置/头顶标记）。
3. **20 局试玩**：每局结束按 1–5 打分。记录自动写到项目根目录 `PlaytestLogs/step1_playtest.csv`（不进 git）。也可以用 `STEP1_PLAYTEST_SHEET.md` 手记。

## 8. 已知限制（统一后修）

- 马里奥移动沿用现有 HeuristicBot（左右 + 跳），追你上台子主要靠它的"目标在头顶就跳"逻辑，可能笨拙。
- 被抓后你是直接传回出生点（没有动画）。
- 马里奥扫描范围 5：扫到伪装的你 → 你现形（这是真的扫描，H5）。
- 沙箱里没有 Unity：编译/物理/手感都只能在你那边确认。纯逻辑（起疑条、状态机、视锥）已在沙箱用 mono 编译并模拟通过。
